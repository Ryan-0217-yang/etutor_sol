count = [0]*26
s = input()
for ch in s:
    if ch.isalpha():  
        ch = ch.lower()  
        index = ord(ch) - ord('a') 
        count[index] += 1
for i in range(26):
    print(count[i],end=" " if i != 25 else '\n')
